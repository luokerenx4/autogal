name: route=neither 应该解锁 005e_lonely 作为唯一可玩 ending
description: |
  跳过前置剧情，直接 seed 一个 "选了拒绝两人" 的 state，
  验证 lonely ending 是唯一能玩的下一段。
state:
  baseline:
    flags:
      route: neither
    completedScripts:
      - 001_meeting_alice
      - 002_meeting_bea
      - 003_invitation
inputs: []
assertions:
  - kind: reason
    eq: inputs-exhausted
  - kind: output
    type: scriptComplete
    present: true
