title: 妖刀奇譚 / Sengoku Raid
preset: ./preset/run.ts
modules:
  - ./modules/raid.ts
switches:
  learnedChinkonho: false
  learnedHayagake: false
variables:
  # Game-level counters. Player stats (hp / mental / spectral / intellect)
  # live on the `player` character's declared stats — see
  # characters/player.md.
  raidsCompleted: { type: number, initial: 0 }
  raidsFailed:    { type: number, initial: 0 }
